services:
  - type: web
    name: crm-ticket-sorter
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false   # Set manually in Render dashboard — never hard-code
      - key: PORT
        value: 3000
